/**
 * @deprecated Import from `@/config/theme` instead.
 * Re-exports preserved for existing imports.
 */
export {
  colors,
  spacing,
  motion,
  measure,
  tagline,
  edition,
} from "@/config/theme";
